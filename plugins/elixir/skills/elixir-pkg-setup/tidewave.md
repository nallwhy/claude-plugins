# tidewave — Additional Setup

After following the official installation guide, configure MCP for Claude Code:

- [ ] Register the MCP server (project scope): `claude mcp add -s project --transport http tidewave http://localhost:$PORT/tidewave/mcp` (replace `$PORT` with the app's port, e.g. 4000)
